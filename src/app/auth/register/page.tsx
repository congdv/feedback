import { LoginButton } from "@/components/auth/login-button";

export default function RegisterPage() {
  return (
    <div>
      <LoginButton>
        <div>Login</div>
      </LoginButton>
    </div>
  )
}