import { LoginButton } from "@/components/auth/login-button";

export default function LoginPage() {
  return (
    <div> 
      <LoginButton>
          <div>Login</div>
      </LoginButton>
    </div>
  )
}